#include "main.h"
#include <stdio.h>

/**
 * _isupper - check if input is an uppercase letter in ASCII
 * @c: integer to check
 * Return: 1 if true, 0 if false
 */
int _ispper(int c)
{
	return (c >= 65 && c <= 90);
}
